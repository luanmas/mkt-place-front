import { Pencil } from "lucide-react";

export default function ButtonEditProduct () {
    return (
        <button type="button" className="p-3 transition ease-in delay-250 hover:bg-blue-500 rounded">
            <Pencil />
        </button>
    )
}